spring:
  datasource:
    url: jdbc:mysql://localhost:3306/articles?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: bigpalmtree02@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true